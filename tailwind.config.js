/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Include all JS, JSX, TS, and TSX files in the src directory
    './components/**/*.{js,jsx,ts,tsx}', // Include all JS, JSX, TS, and TSX files in the components directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

